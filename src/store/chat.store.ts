// import type { UserObj } from "@/types/Response";
// import { create } from "zustand";

// const chatStore = create((set) => ({
//   chat: {
//     participants:[]
//   },
//   setParticipants: (newParticipants: UserObj[]) =>
//     set({ participants: newParticipants }),
//   clearParticipants: () => set({ participants: [] }),
// }));

// export { chatStore };
